#ifndef UTILS_HPP
#define UTILS_HPP

#include <iostream>

template <typename T> T getInput(const char *msg);

template <typename T> T findMin(const T *begin, const T *end);

template <typename T> T inputArray(T *arr, int size);

template <typename T> T printArray(const T *arr, int size);

#endif